general:
  base_dir: D:/Video/test
  input: tracks_relabel/*.csv
  video_input: '*.avi.mp4'

operations:
  - relabel:
      method: sort area
      input: tracks/*.csv
      output: tracks_relabel

#  - relabel_video:
#      video_output: annotated.mp4

  - extract_features:
      - profiles:
          features: [v, vangle]
          output: 'profile_{feature}.csv'
      - events:
          features: [n, time, delta_time]
          output: events.csv
      - activity:
          features: [movement]
          output: '{feature}_dataframe.csv'
